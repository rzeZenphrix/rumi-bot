const crypto=require('node:crypto'); const respond=require('../../utils/respond');
module.exports={name:'password',aliases:['pwgen'],category:'utility',description:'I generate a secure password.',usage:'password [length]',examples:['password 24'],async execute({message,args}){const len=Math.max(8,Math.min(64,Number(args[0])||20)); const chars='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%^&*_-+='; let out=''; for(let i=0;i<len;i++)out+=chars[crypto.randomInt(chars.length)]; return respond.reply(message,'info',null,{description:`🔐 **Generated password**\n||${out}||`});}};
