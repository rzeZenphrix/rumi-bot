const respond=require('../../utils/respond');
module.exports={name:'color',aliases:['colour','hex'],category:'utility',description:'I show color info from a hex value.',usage:'color <hex>',examples:['color #5865f2'],async execute({message,args}){const raw=(args[0]||'').replace('#',''); if(!/^[0-9a-f]{6}$/i.test(raw))return respond.reply(message,'info','send a valid hex color like `#5865f2`.'); const r=parseInt(raw.slice(0,2),16),g=parseInt(raw.slice(2,4),16),b=parseInt(raw.slice(4,6),16); return respond.reply(message,'info',null,{color:parseInt(raw,16),description:`🎨 **Color info**\n**HEX:** \`#${raw.toUpperCase()}\`\n**RGB:** \`${r}, ${g}, ${b}\``});}};
