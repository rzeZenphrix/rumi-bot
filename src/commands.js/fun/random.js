const respond=require('../../utils/respond');
module.exports={name:'random',aliases:['rand','number'],category:'fun',description:'I return a random number.',usage:'random [min] [max]',examples:['random 1 100'],async execute({message,args}){let min=Number(args[0]||1), max=Number(args[1]||100); if(!Number.isFinite(min)||!Number.isFinite(max))return respond.reply(message,'info','use `random [min] [max]`.'); if(min>max)[min,max]=[max,min]; const n=Math.floor(Math.random()*(max-min+1))+min; return respond.reply(message,'info',null,{description:`🎲 **Random number**\n\`${n}\``});}};
