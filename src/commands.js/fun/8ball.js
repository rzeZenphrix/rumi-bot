const respond=require('../../utils/respond'); const answers=['yes','no','maybe','definitely','not today','ask again later','the signs say yes','I would not count on it'];
module.exports={name:'8ball',aliases:['8b'],category:'fun',description:'I answer a question randomly.',usage:'8ball <question>',examples:['8ball will this work?'],async execute({message,args}){if(!args.length)return respond.reply(message,'info','ask me a question.'); return respond.reply(message,'info',null,{description:`🎱 **8ball**\n${answers[Math.floor(Math.random()*answers.length)]}`});}};
