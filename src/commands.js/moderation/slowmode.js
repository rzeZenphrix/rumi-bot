const { PermissionFlagsBits } = require('discord.js'); const respond=require('../../utils/respond');
module.exports={name:'slowmode',aliases:['slow'],category:'moderation',description:'I set channel slowmode.',usage:'slowmode <seconds|off>',examples:['slowmode 5','slowmode off'],guildOnly:true,permissions:[PermissionFlagsBits.ManageChannels],botPermissions:[PermissionFlagsBits.ManageChannels],async execute({message,args}){const raw=(args.shift()||'').toLowerCase(); const seconds=raw==='off'?0:Math.max(0,Math.min(21600,Number(raw))); if(!Number.isFinite(seconds))return respond.reply(message,'info','use `slowmode <seconds|off>`.'); await message.channel.setRateLimitPerUser(seconds,`Slowmode by ${message.author.tag}`); return respond.reply(message,'good',seconds?`set slowmode to **${seconds}s**.`:'turned slowmode off.');}};
