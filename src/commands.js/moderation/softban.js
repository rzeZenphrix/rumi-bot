const { PermissionFlagsBits } = require('discord.js'); const respond=require('../../utils/respond'); const { resolveUser }=require('../../utils/resolveUser');
module.exports={name:'softban',aliases:['sb'],category:'moderation',description:'I ban, clean messages, then unban a user.',usage:'softban <user> [reason]',examples:['softban @user spam'],guildOnly:true,permissions:[PermissionFlagsBits.BanMembers],botPermissions:[PermissionFlagsBits.BanMembers],async execute({client,message,args}){const user=await resolveUser(client,args.shift()); if(!user)return respond.reply(message,'bad','I could not find that user.'); const reason=args.join(' ')||`Softban by ${message.author.tag}`; await message.guild.members.ban(user.id,{deleteMessageSeconds:86400,reason}); await message.guild.members.unban(user.id,`Softban release: ${reason}`).catch(()=>null); return respond.reply(message,'good',`softbanned ${user} and cleaned recent messages.`);}};
